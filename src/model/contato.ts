//Para da criação da lista interface que iremos utilizar para definir os fundamentos 
interface Contato { 
    id : string | null;
    nome : string;
    telefone : string;
    email : string;
}

//vai levar para uma tela de um produto que é o produto screen

export {Contato};